const student = { 
    name: 'John',
    age: 20,
    hobbies: ['reading', 'games', 'coding'],
};


for (let key in student) { 
  
     student[key];

    console.log(key + " - " +  student[key]); 
} 