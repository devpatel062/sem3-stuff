var number = prompt("")
        var count=0;
        
        for( var i =1;i < number ; i++)
        {
            if(number % i == 0)
                console.log(i+" ")
        }
        console.log(number+" ")